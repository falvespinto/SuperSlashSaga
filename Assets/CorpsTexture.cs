using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CorpsTexture : MonoBehaviour
{
    public Material materialYuetsu;
    public Texture textureYuetsu;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
